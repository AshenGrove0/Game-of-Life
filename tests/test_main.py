import pytest
from main import *
'''

def test_parse_args() -> tuple[str, dict, float]:
def test_check_empty_file(file: str) -> bool:
def test_fetch_starting_coords(file: str, random:bool, board_diamensions:dict) -> list:

def test_fetch_coords_from_coords(raw:list) -> list:
def test_fetch_coords_from_pattern(raw:list) -> list:
def test_generate_starting_board(alive_coords_to_start: list, board_diamensions: dict) -> list:
def test_get_neighbour_count(board: list, row: int, column: int) -> int:
'''